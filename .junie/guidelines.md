`../AGENTS.md` を最優先で参照してください。

Junie では特に次の点を守ってください。

- 授業教材なので、説明もコードもシンプルに保つ
- Java 標準ライブラリのみを使い、外部ライブラリは追加しない
- コメントや補足説明は日本語で書く
- `src/` のコードはデフォルトパッケージの独立実行プログラムとして扱う
- 命名は既存の流れに合わせる
- `Main`、`TestNNNN`、`ArithNN`、`ConcatNN` を基本の命名パターンとして扱う
- コンパイルエラーの説明例は、既存コードと同様にコメントアウトして残してよい
